#include <stdio.h>
#define END 500009
int k, n, ind;
char sta[END+1], a[END+1];
int main() {
    int i;
    scanf("%d %d", &n, &k);
    scanf("%s", a);
    for(i=0;a[i];i++) {
        if(!ind||) {

        }   
        else {

        }
    }
    printf("%s", sta);
}