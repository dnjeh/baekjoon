#include <stdio.h>
int a[1000001], n, m;
void set();

int main() {
    int i, j, m, t1, t2;
    scanf("%d %d", &n, &m); set();   
    for(i=0;i<m;i++) {
        scanf("%d %d %d", &m, &t1, &t2);
        if(!m) {

        }
        else {
            
        }
    }
}

void set() {
    int i;
    for(i=0;i<=n;i++) a[i]=i;
}