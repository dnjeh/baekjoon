#include <stdio.h>
int main() {
    int n, m, i, j, cnt=0, x, y;
    scanf("%d %d", &n, &m);
    int a[n][m];
    for(i=0;i<n;i++) for(j=0;j<m;j++) a[i][j]=0;
    for(x=y=0;cnt<n*m;cnt++) {
        
    }
}