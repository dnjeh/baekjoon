#include <stdio.h>
#include <string.h>
#define SIZ 500100
int a[SIZ], sta, end, siz, bcnt, wcnd, dir; 
int main() {
    char t[10], tt;
    int q;
    scanf("%d", &q);
    for(int i=0;i<q;i++) {
        scanf(" %s", t);
        if(strcmp(t, "pop")) {
            scanf(" %c", &tt);
        }
        else {
            int it=pop();
            if(!it&&dir==)
        }
    }
    return 0;
}
